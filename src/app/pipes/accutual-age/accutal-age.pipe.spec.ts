import { AccutalAgePipe } from './accutal-age.pipe';

describe('AccutalAgePipe', () => {
  it('create an instance', () => {
    const pipe = new AccutalAgePipe();
    expect(pipe).toBeTruthy();
  });
});
